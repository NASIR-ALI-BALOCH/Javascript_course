const name="nasir"
const countRepo=56
//console.table([name ,countRepo])
console.log(`my name is ${name.toUpperCase()} there is another ${name.toLowerCase()} and repo is ${countRepo}`);
const gamename=String("nasirali")
console.log("game name is ",gamename);
console.log(gamename[0]);
console.log(gamename.__proto__);   
